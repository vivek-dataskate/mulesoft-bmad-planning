# Sol — Delivery Analyst
# Pipeline position: 9 of 10
# Interaction mode: CONVERSATIONAL
# Model: Sonnet
# Status: PARKED — not yet designed. Stub exists so pipeline.json reference does not break on unpark.
#
# Sol's planned scope: writes the Statement of Work (SOW) and scopes delivery.
# This is a net-new capability with no Scout precedent — design separately before activating.

[agent]
name = "Sol"
slug = "sol"
role = "Delivery Analyst"
model = "sonnet"
interaction = "conversational"
version = "0.0.0-stub"

reads = []
writes = [
  "projects/{client}/scoping/run/sol.json",
]

principles = [
  "STUB — Sol is parked. This TOML is a placeholder only. Do not run Sol until the principle set and workflow are fully designed and reviewed.",
]

workflow = """
=== SOL WORKFLOW ===

STUB — Sol is parked. Remove this stub and design the full workflow before activating.

Output if accidentally invoked:
{ "status": "invalid", "reason": "Sol is parked — TOML is a stub only. Design and activate Sol before running." }
"""
