vertical: commerce
client: agilemind
generatedBy: scout
generatedAt: "2026-05-13"
notes: >
  AgileMind is an educational publisher. Their business objects (products/ISBNs, agreements/contracts,
  invoices, inventory) map to the OAGIS 10.x commerce vertical. Key deviation: products have
  regional ISBN variants (same program title, different ISBN per state). This is not covered by
  standard OAGIS SyncItem BOD and is tracked as addedFields.

extensions:
  - baseModel: standards/canonical-models/commerce/canonical-invoice.yaml
    record: Invoice
    addedFields: []
    renamedFields:
      - canonical: invoiceId
        client: QB_InvoiceId__c
        clientFormat: "QuickBooks invoice number (auto-generated by QB)"
        sourceSystem: QuickBooks Online
      - canonical: customerId
        client: Account.Id (Salesforce)
        clientFormat: "Salesforce Account record ID"
        sourceSystem: Salesforce
    omittedFields: []
    status: stub — to be confirmed by Analyst

  - baseModel: standards/canonical-models/commerce/canonical-order.yaml
    record: Agreement (Salesforce custom object maps to Order)
    addedFields:
      - name: agreementStatus
        type: string
        description: Agreement lifecycle status in Salesforce (Draft, Approved, Active, Closed)
        sourceSystem: Salesforce
        requiredForClient: true
      - name: schoolStartDate
        type: date
        description: Target school start date — drives fulfillment sequencing (West to East rule)
        sourceSystem: Salesforce
        requiredForClient: true
      - name: fiscalYear
        type: string
        description: Academic/fiscal year label (e.g. "AY2026-2027") — multi-year contract tracking
        sourceSystem: Salesforce
        requiredForClient: true
      - name: invoiceDueDate
        type: date
        description: QB invoice due date — set per invoicing scenario (early orders due July/Aug; summer orders due 30 days)
        sourceSystem: QuickBooks Online
        requiredForClient: true
    renamedFields:
      - canonical: orderId
        client: Agreement__c.Id / Name
        clientFormat: "Salesforce Agreement record ID"
        sourceSystem: Salesforce
    omittedFields: []
    status: stub — to be confirmed by Analyst

  - baseModel: standards/canonical-models/commerce/canonical-product.yaml
    record: Inventory Item / Print Book
    addedFields:
      - name: isbn
        type: string
        description: International Standard Book Number — primary key for physical inventory tracking
        sourceSystem: QuickBooks Online
        requiredForClient: true
      - name: isbnVariants
        type: array
        description: State/region-specific ISBN variants for the same program title (e.g. CA vs National version)
        sourceSystem: Salesforce (ISBN lookup table)
        requiredForClient: true
      - name: committedQuantity
        type: integer
        description: Quantity committed to existing closed-won agreements (unavailable for new orders)
        sourceSystem: Salesforce (custom Inventory object — to be created)
        requiredForClient: true
      - name: availableQuantity
        type: integer
        description: On-hand quantity minus committed quantity
        sourceSystem: Calculated
        requiredForClient: true
      - name: bufferPct
        type: float
        description: Safety buffer percentage per title (e.g. 3% for Intensified Algebra Volume 1)
        sourceSystem: Holly Wale demand worksheet
        requiredForClient: false
    renamedFields:
      - canonical: productId
        client: ISBN
        clientFormat: "13-digit ISBN"
        sourceSystem: QuickBooks Online
    omittedFields: []
    status: stub — to be confirmed by Analyst
