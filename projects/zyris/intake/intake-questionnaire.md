# Zyris — MuleSoft Intake Questionnaire

Generated by Scout — Scoping Analyst
Source: Pre-sales call transcript, Feb 17 2026
Date: 2026-05-11

HOW TO FILL THIS IN: Read each item. If an answer is already pre-filled from our Feb 17 call, leave it to confirm or overwrite it to correct. If the Answer line is blank, please provide your answer. Write UNKNOWN if you do not have the answer yet. Return the completed form with the address normalization flowchart attached.

---

SECTION 1: USE CASES

One sub-section per integration flow we discussed. Each covers what we understood plus the open questions specific to that flow.


USE CASE 1 — Contact and Customer Sync
Systems: HubSpot and D365 Finance and Operations
Direction: Bidirectional — HubSpot to D365 FO as primary, D365 FO record ID written back to HubSpot
Trigger: Scheduled batch job on a regular interval, plus a real-time sync button in HubSpot

What we understood:
- MuleSoft queries HubSpot for contacts in a specific lifecycle stage that have been modified since the last sync
- Records are looked up in D365 FO and upserted — updated if found, created if not
- Once the record is created or updated in D365 FO, the FO record ID is stamped back onto the HubSpot contact to link the two records

Is this correct?
Answer: Yes — [write any corrections here]

Q1.1. Which HubSpot lifecycle stage or stages should trigger a contact to sync to D365 FO?
Answer:

Q1.2. How often should the scheduled batch job run?
Answer: Hourly was mentioned as an example on the call — please confirm or specify a different interval.

Q1.3. If there is a conflict between the HubSpot and D365 FO versions of a record, which system wins?
Answer: HubSpot assumed as the master CRM — correct if wrong.


---

USE CASE 2 — Order Creation
Systems: Shopify to D365 Finance and Operations
Direction: Shopify to D365 FO, with the D365 FO order ID written back to Shopify
Trigger: New order placed in Shopify

What we understood:
- When a new order is placed in Shopify, MuleSoft receives that event
- Address normalization logic is applied to identify the correct D365 FO customer record (see attachment request below)
- The order is created in D365 FO following your existing business rules
- Once the FO order is created, the FO order ID is written back into Shopify

Is this correct?
Answer: Yes — [write any corrections here]

Q2.1. Should MuleSoft receive new order events via a Shopify webhook (Shopify pushes in real time when an order is placed) or by polling the Shopify Orders API on a schedule?
Answer:

Q2.2. Does the order sync include line items and product or SKU details, or just the order header?
Answer:

ATTACHMENT REQUIRED: The address normalization flowchart you previously sent to the MuleSoft team is needed to finalize the business logic for this use case. Please attach it when returning this form.
Attached: Yes / No


---

USE CASE 3 — Order Status Sync
Systems: D365 Finance and Operations to Shopify
Direction: D365 FO to Shopify
Trigger: Order status update in D365 FO

What we understood:
- When an order status changes in D365 FO (e.g. fulfilled, delivered), MuleSoft captures that update and pushes it to the corresponding Shopify order
- Whether this runs in real time or as a batch job was left open

Is this correct?
Answer: Yes — [write any corrections here]

Q3.1. Should this run in real time or on a schedule? If scheduled, how often?
Answer:

Q3.2. What status fields need to sync to Shopify — status only, or also tracking numbers and fulfilment dates?
Answer:


---

USE CASE 4 — Merge Resolution
Systems: HubSpot to HubSpot and D365 Finance and Operations
Direction: HubSpot fires the event; MuleSoft updates both HubSpot and D365 FO
Trigger: HubSpot contact merge webhook event

What we understood:
- When duplicates are merged in HubSpot, MuleSoft receives the merge event
- MuleSoft ensures the surviving HubSpot record retains all external IDs from the merged records — specifically the D365 FO customer ID and the Shopify customer ID
- Alex confirmed that MuleSoft should also trigger a corresponding merge of related records in D365 FO

Is this correct?
Answer: Yes — [write any corrections here]

Q4.1. Should the D365 FO merge happen automatically when a HubSpot merge is received, or should it go to a manual review queue first?
Answer: Automatic assumed based on Alex's confirmation on the call — correct if wrong.


---

USE CASE 5 — Customer Tag and Pricing Sync
Systems: D365 Finance and Operations or HubSpot (source) to Shopify (target)
Direction: Source system to Shopify
Trigger: Sales agreement or pricing update in D365 FO or HubSpot

What we understood:
- When a sales agreement tag or pricing update is made, MuleSoft pushes the relevant tags or terms to the matching Shopify customer record
- Which system is the source of truth was left open on the call and needs to be decided before architecture begins
- Whether this runs in real time or as a batch job was also left open

Is this correct?
Answer: Yes — [write any corrections here]

Q5.1. Which system is the source of truth for sales agreement tags and pricing — D365 FO, HubSpot, or both?
Answer: Left open on the call — please decide before returning this form.

Q5.2. Should this run in real time or on a schedule? If scheduled, how often?
Answer:

Q5.3. Are pricing and sales agreement tags the only tags on Shopify customer records, or do other tags exist alongside them that must be preserved when MuleSoft updates them?
Answer:


---

SECTION 2: SYSTEMS AND ACCESS

Q6. For D365 Finance and Operations — is this the cloud-hosted version (Microsoft online) or on-premises?
Answer:

Note: HubSpot and Shopify are assumed to be cloud SaaS. Correct either if wrong.

Q7. Do you have a test or sandbox environment for each system that the implementation team can use during development?

HubSpot sandbox:
Answer:

D365 FO sandbox:
Answer:

Shopify development store:
Answer:

Q8. What tools are currently handling your integrations? The call mentioned fragmented point-to-point integrations — please name the specific tools (Zapier, custom HubSpot workflows, custom scripts, other).
Answer:

Q9. Is there anything already built on Anypoint Exchange that we should be aware of or reuse?
Answer: No — new platform, new subscription. Correct if wrong.


---

SECTION 3: DATA AND FIELD MAPPING

We have mapped the standard fields for all three systems against their public APIs. The table below shows the mappings we are confident in. Only the gaps below require your input.

Standard field mappings we already know:

HubSpot Contact → D365 FO Customer
  HubSpot email → D365 FO PrimaryContactEmail
  HubSpot firstname + lastname → D365 FO OrganizationName (concatenated)
  HubSpot phone → D365 FO PrimaryContactPhone
  HubSpot address → D365 FO AddressStreet
  HubSpot city → D365 FO AddressCity
  HubSpot state → D365 FO AddressState
  HubSpot zip → D365 FO AddressZipCode
  HubSpot country (free text) → D365 FO AddressCountryRegionISOCode (ISO alpha-2) — NOTE: HubSpot stores country as free text; conversion to ISO code required
  HubSpot hs_object_id → stored as a custom field on D365 FO customer (field name unknown — see Q11)

Shopify Order → D365 FO Sales Order
  Shopify order id → stored in D365 FO (field name unknown — see Q11)
  Shopify customer.id → D365 FO CustomerAccountNumber (via customer lookup)
  Shopify financial_status / fulfillment_status → D365 FO SalesOrderStatus (mapping table required — see Q13)
  Shopify line_items[].variant_id or .sku → D365 FO ItemNumber (mapping confirmation required — see Q12)
  Shopify line_items[].quantity → D365 FO SalesQuantity
  Shopify line_items[].price → D365 FO SalesPrice
  Shopify shipping_address → D365 FO delivery address fields

Q10. D365 FO does not have a standard field for storing external system IDs. Your D365 FO administrator must create custom extension fields via AOT extensions. What are the field names for:

HubSpot contact ID field on D365 FO customer:
Answer:

Shopify customer ID field on D365 FO customer:
Answer:

D365 FO order number field on Shopify order (note_attribute key):
Answer:

HubSpot contact ID and Shopify customer ID as custom properties in HubSpot — what are the internal property names?
Answer:

Q11. What is the default value for CustomerGroupId when creating a new D365 FO customer? This field is required on create and the default varies per D365 FO configuration. Your D365 FO admin can confirm.
Answer:

Q12. Shopify line items identify products by variant_id and SKU. D365 FO uses its own ItemNumber. How do these map — is it 1-to-1 by SKU, by variant_id, or is there a separate product mapping table?
Answer:

Q13. Shopify fulfillment_status (null/partial/fulfilled) and D365 FO SalesOrderStatus (None/Backorder/Delivered/Invoiced/Cancelled) do not map 1-to-1. Please provide the mapping table your business requires:

Shopify null (unfulfilled) → D365 FO:
Answer:

Shopify partial → D365 FO:
Answer:

Shopify fulfilled → D365 FO:
Answer:

D365 FO Invoiced → Shopify:
Answer:

D365 FO Cancelled → Shopify:
Answer:

Q14. Are there known data quality issues in any system — for example duplicate contacts in HubSpot, inconsistent address formats in Shopify, or orders with missing required fields in D365 FO?
Answer:


---

SECTION 4: VOLUME AND PERFORMANCE

Q15. Approximate volumes per flow:

Contacts synced per UC1 batch run:
Answer:

Real-time UC1 sync requests per day:
Answer:

New Shopify orders per day (UC2):
Answer:

Order status updates per day (UC3):
Answer:

HubSpot merge events per week (UC4):
Answer:

Tag or pricing updates per day (UC5):
Answer:

Q16. Are there peak periods we should design for — for example promotional campaigns, month-end close, or fiscal year-end?
Answer:

Q17. Maximum acceptable wait time for the real-time sync button in Use Case 1:
Answer: Under 1 second / Under 3 seconds / Under 10 seconds / Async is fine

Q18. What uptime is required for the integration flows?
Answer: Best-effort / 99.9% (less than 9 hours downtime per year) / 99.99% (less than 1 hour per year)


---

SECTION 5: SECURITY AND COMPLIANCE

Q19. Personal data is being transmitted in Use Cases 1 and 2 — contact names, email addresses, and postal addresses. Are there additional sensitive fields such as payment data flowing through MuleSoft, or does payment data stay within Shopify?
Answer:

Q20. Are there regulatory compliance requirements — for example GDPR if EU customers are in scope, CCPA for California, or PCI-DSS if payment data passes through MuleSoft?
Answer:

Q21. Are there data residency requirements — must any data stay within a specific country or region?
Answer:

Q22. For HubSpot authentication — will you use a Private App token or an OAuth2 app? Private App tokens are recommended as legacy API keys are deprecated.
Answer:


---

SECTION 6: ERROR HANDLING

Q23. If D365 FO or Shopify is temporarily unavailable, should MuleSoft retry automatically, hold the message in a queue, or fail immediately?
Answer: Retry with queue is the standard recommendation — confirm or specify preference.

Q24. Who should be notified when an integration fails, and how — email, Slack, or another channel?
Answer:

Q25. For Use Case 2 — if the Shopify order event is received but the D365 FO order creation fails, what should happen? Is retrying later acceptable, or does the Shopify order need to be cancelled automatically?
Answer:

Q26. Are there flows where processing the same event twice would cause a business problem — for example creating a duplicate order in D365 FO?
Answer: Yes assumed for UC2 — confirm and flag any other flows.


---

SECTION 7: DEPLOYMENT AND DEVOPS

Q27. CloudHub 2.0 is the standard deployment model for Anypoint Platform. Is that correct for Zyris?
Answer: Yes assumed — correct if wrong.

Q28. Which environments are needed — dev, UAT, prod, or others?
Answer: Dev, UAT, and prod assumed as standard — confirm or add any others.

Q29. Are there network or firewall restrictions we should know about — for example D365 FO behind a VPN, outbound firewall rules, or IP whitelisting requirements?
Answer:

Q30. What CI/CD tools does your team use?
Answer:

Q31. Do you have a secrets management solution — Azure Key Vault, AWS Secrets Manager, HashiCorp Vault? If not, Anypoint's built-in secrets management will be used.
Answer:


---

SECTION 8: OPERATIONS AND SUPPORT

Q32. What monitoring or logging tools does your team already use — Splunk, Datadog, Azure Monitor, or other? Or will you rely on the Anypoint Monitoring dashboards shown in the demo?
Answer: Anypoint Monitoring assumed — correct if wrong.

Q33. Who will own integration support after go-live — the internal team, the implementation partner on retainer, or primarily MuleSoft Premier Success for escalations?
Answer: Internal team (Alex and Rafael) with Premier Success for escalations assumed — correct if wrong.

Q34. Rodrigo mentioned he liked the analytics and tracking in the demo. Do you need a business-facing dashboard showing integration activity — for example orders synced today or last sync time — without needing to log into Anypoint?
Answer:


---

SECTION 9: TESTING AND GO-LIVE

Q35. Does your team have any automated testing capability today — Postman collections, integration test scripts, unit tests — or are we starting from zero?
Answer:

Q36. What would a successful UAT look like — for example a specific number of contacts synced correctly, or an end-to-end order flowing through within a certain time?
Answer:

Q37. Are there blackout periods when no changes can be deployed — for example month-end close or a major promotional event in Shopify?
Answer:

Q38. What is the target go-live date for the first flow? Are there any hard deadlines?
Answer:


---

SECTION 10: SYSTEM-SPECIFIC DETAILS

These questions are for your technical team or system administrator.

HubSpot

Q39. Which HubSpot subscription tier are you on — Starter, Professional, or Enterprise?
Note: Webhook subscriptions for Use Case 4 require Professional or higher.
Answer:

---

D365 Finance and Operations

Q40. What is your D365 FO environment URL?
Answer: (typically in the format your-org.operations.dynamics.com)

Q41. What OData entity names are used for customer records and sales orders in your environment? Your D365 FO administrator or Microsoft partner can confirm.

Customer entity:
Answer:

Sales order entity:
Answer:

---

Shopify

Q42. Which Shopify plan are you on — Basic, Shopify, Advanced, or Plus?
Note: This affects API rate limits and how batch flows are designed.
Answer:


---

INTERNAL FLAGS — DO NOT SEND TO CLIENT

FLAG-1: D365 FO connector does not support PATCH on OData entities (FK-005, confirmed). UC1 upsert, UC3 status write-back, and UC4 FO merge all require a manual OAuth token subflow. Budget 1-2 extra story points per affected flow.

FLAG-2: HubSpot connector (mule-hubspot-connector 1.0.0) in registry conflicts with Sam McKay saying no pre-built connector on the Feb 17 call. Verify on Exchange before committing. If partial coverage, fall back to HTTP connector with manual token management.

FLAG-3: HubSpot search API uses cursor-based pagination (paging.next.after). Mule 4 has no native while-loop. Use recursive sub-flow for under 1000 contacts per run, batch scope with page-aware iterator above that (FK-006).

FLAG-4: UC4 merge event is a webhook flow. Full 5-step idempotency pattern from FK-008 is mandatory — always return 200 to HubSpot, dedup by eventId before processing, store eventId only after all writes succeed, route failures to DLQ.

FLAG-5: UC1 watermark must advance after the foreach loop completes successfully, not before. Pre-advancing causes silent data loss if processing fails mid-batch (FK-007).

FLAG-6: Address normalization flowchart is missing from the scoping folder. UC2 architecture cannot be finalized without it. Block the architect until it arrives.

FLAG-7: Shopify connector has partial coverage for fulfillment operations (FK-005). If UC3 requires updating fulfillment status in Shopify, raw HTTP calls with a manual token subflow will be needed.
