<?xml version="1.0" encoding="UTF-8"?>
<!--
    {{FLOW_FILE_NAME}}.xml — Generated by scaffold generator.
    Flow group: {{FLOW_DESCRIPTION}}
    Source: {{FLOW_SOURCE}} → Target: {{FLOW_TARGET}}
    Pattern: {{PRIMARY_PATTERN}} | Layer: {{API_LAYER}}
    Trigger: {{TRIGGER_TYPE}}

    Rules enforced:
      - This file contains ONLY flows — no connector configs (those live in global-config.xml)
      - All DataWeave transforms > 10 lines live in resources/dwl/ — never inline
      - correlationId is set once at entry point; never regenerated mid-flow
      - All flows reference Global_Error_Handler
-->
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:anypoint-mq="http://www.mulesoft.org/schema/mule/anypoint-mq"
      {{ADDITIONAL_NAMESPACES}}
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core
          http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http
          http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core
          http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/os
          http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
        http://www.mulesoft.org/schema/mule/anypoint-mq
          http://www.mulesoft.org/schema/mule/anypoint-mq/current/mule-anypoint-mq.xsd
        {{ADDITIONAL_SCHEMA_LOCATIONS}}">

    {{FLOW_CONTENT}}

</mule>
