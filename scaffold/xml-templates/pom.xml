<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>{{groupId}}</groupId>
    <artifactId>{{artifactId}}</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>mule-application</packaging>
    <name>{{artifactId}}</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <app.runtime>4.8.0</app.runtime>
        <mule.maven.plugin.version>4.2.0</mule.maven.plugin.version>
        <munit.version>3.1.0</munit.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.mule.tools.maven</groupId>
                <artifactId>mule-maven-plugin</artifactId>
                <version>${mule.maven.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <classifier>mule-application</classifier>
                    <!--
                        CloudHub 2.0 deployment — uncomment and set secrets in CI/CD.
                        Required GitHub Actions secrets:
                          ANYPOINT_CONNECTED_APP_CLIENT_ID
                          ANYPOINT_CONNECTED_APP_CLIENT_SECRET
                        Required GitHub Actions variables:
                          ANYPOINT_ENVIRONMENT (dev | uat | prod)
                    -->
                    <!--
                    <cloudhub2Deployment>
                        <uri>https://anypoint.mulesoft.com</uri>
                        <provider>MC</provider>
                        <environment>${anypoint.environment}</environment>
                        <target>Cloudhub-{{awsRegion}}</target>
                        <applicationName>{{artifactId}}</applicationName>
                        <muleVersion>${app.runtime}</muleVersion>
                        <replicas>1</replicas>
                        <replicaSize>MICRO</replicaSize>
                        <integrations>
                            <services>
                                <objectStoreV2>
                                    <enabled>true</enabled>
                                </objectStoreV2>
                            </services>
                        </integrations>
                        <connectedAppClientId>${anypoint.connectedApp.clientId}</connectedAppClientId>
                        <connectedAppClientSecret>${anypoint.connectedApp.clientSecret}</connectedAppClientSecret>
                        <connectedAppGrantType>client_credentials</connectedAppGrantType>
                    </cloudhub2Deployment>
                    -->
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.mulesoft.munit.tools</groupId>
                <artifactId>munit-maven-plugin</artifactId>
                <version>${munit.version}</version>
                <executions>
                    <execution>
                        <id>test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                            <goal>coverage-report</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <coverage>
                        <runCoverage>true</runCoverage>
                        <failBuild>true</failBuild>
                        <!-- Coverage floor set per pattern — see decisions.json scaffold.munitCoverage -->
                        <requiredApplicationCoverage>{{munitCoverage}}</requiredApplicationCoverage>
                        <requiredResourceCoverage>{{munitCoverage}}</requiredResourceCoverage>
                        <requiredFlowCoverage>{{munitCoverage}}</requiredFlowCoverage>
                        <formats>
                            <format>console</format>
                            <format>html</format>
                        </formats>
                        <!-- Connector operations are excluded from coverage — test logic, not connector -->
                        <ignoreFlows>
                            <ignoreFlow>.*-config.*</ignoreFlow>
                        </ignoreFlows>
                    </coverage>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <!-- MUnit — always included, test scope only -->
        <dependency>
            <groupId>com.mulesoft.munit</groupId>
            <artifactId>munit-runner</artifactId>
            <version>${munit.version}</version>
            <classifier>mule-plugin</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.mulesoft.munit</groupId>
            <artifactId>munit-tools</artifactId>
            <version>${munit.version}</version>
            <classifier>mule-plugin</classifier>
            <scope>test</scope>
        </dependency>

        {{CONNECTOR_DEPENDENCIES}}
        <!--
            CONNECTOR_DEPENDENCIES block generated from decisions.json systems.connectors[]
            Each entry: TODO comment pointing to Exchange URL for exact patch version.
            Format per connector:
              <dependency>
                  <groupId>{groupId}</groupId>
                  <artifactId>{artifactId}</artifactId>
                  <version>{version}</version>
                  <classifier>mule-plugin</classifier>
              </dependency>
        -->
    </dependencies>

    <repositories>
        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
            <layout>default</layout>
        </repository>
        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </pluginRepository>
    </pluginRepositories>
</project>
