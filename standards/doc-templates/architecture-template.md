# Architecture — {Client Name}
> Generated by: BMAD Architect Agent
> Date: {YYYY-MM-DD}
> Architect: {name or "BMAD Architect"}
> Status: DRAFT | READY-FOR-PM | APPROVED

---

## Decision Summary

| Level | Decision | Value |
|-------|----------|-------|
| Level 0 — Integration Style | {messaging / rpc / file-transfer / shared-db / hybrid} | |
| Level 1 — Primary Pattern | {A–R pattern name} | See: standards/scenarios/{file}.md |
| Level 1 — Secondary Patterns | {none / list} | |
| Level 2 — Volume | {low / medium / high / bulk} | |
| Level 2 — Latency | {under-1s / under-3s / under-10s / async-ok} | |
| Level 2 — Availability | {best-effort / 99.9 / 99.99} | |
| Level 3 — Connectors | {list connector keys} | See: standards/connector-registry.json |
| Level 4 — Message TTL | {N} hours | {event category: critical / standard / notification / CDC / monitoring} |
| Level 4 — Compensation Strategy | {write-off / retry / compensating-transaction} | {per dominant flow type; see per-flow sections for exceptions} |
| Level 4 — Operational Needs | {list selected options} | |
| Level 5 — Security | {internal / partner / regulated / government} | |
| Level 6 — Client-Facing | {list or "none — backend only"} | |
| Scaffold Profile | {minimal / standard / enterprise / regulated} | |
| AI Integration | {enabled / disabled} | {provider + use case if enabled} |

---

## Level 0 — Integration Style

**Selected:** {messaging | rpc | file-transfer | shared-db | hybrid}

**Rationale:**
{1-3 sentences. Why this style fits the client's trigger model and NFRs. Reference the selection rule from PLANNING_CONTEXT.md that applies.}

---

## Level 1 — Primary Pattern

**Selected:** {pattern letter} — {pattern name}
**Reference scenario:** `standards/scenarios/{filename}.md`

**Rationale:**
{Why this pattern was selected over alternatives. Name the runner-up pattern(s) and why they were rejected.}

**Secondary Patterns** (if hybrid):
- {pattern letter} — {pattern name}: {why needed as secondary}

**EDA Fit Assessment** (required when Messaging style selected):

| Question | Answer | Implication |
|---------|--------|-------------|
| Publisher and consumer teams evolve independently? | Yes / No | |
| New consumers will be added without publisher changes? | Yes / No | |
| Consumer needs event replay? | Yes / No | Kafka required if Yes |
| Throughput > 100K/day or consumers > 10? | Yes / No | Kafka required if Yes |
| Publisher is internal and under your control? | Yes / No | |
| Each consumer needs independent delivery guarantee? | Yes / No | |

**EDA verdict:** {Choose one:}
- Warranted — topology decoupling has real value (publisher/consumer evolve independently)
- Not warranted — point-to-point async is simpler (same team controls both ends)
- Kafka required — throughput >100K/day OR consumers >10 OR event replay needed (Anypoint MQ insufficient)

---

## Level 2 — NFR Profile

| Dimension | Value | Notes |
|-----------|-------|-------|
| Volume | {low / medium / high / bulk} | {record count / event rate} |
| Latency | {under-1s / under-3s / under-10s / async-ok} | {source of requirement} |
| Frequency | {real-time / scheduled / triggered / one-time} | |
| Availability | {best-effort / 99.9 / 99.99} | |
| Throughput | {low / medium / high / very-high} | |
| Peak Period | {description} | |

---

## Level 3 — Systems and Connectors

| System | Role | Connector Key | Version | Auth | Registry Status |
|--------|------|--------------|---------|------|----------------|
| {system} | source / target / both | {key from registry} | {version} | {auth type} | Verified {date} / MCP-checked {date} |

**Custom Systems** (not in Exchange):
| System | Integration Method | Notes |
|--------|------------------|-------|
| {system} | HTTP (REST) / SOAP / SFTP | {custom-{name} entry added to registry} |

**API Discovery Required:**
| System | Contract File | Status |
|--------|--------------|--------|
| {system} | projects/{client}/api-discovery/{system}-contract.md | Complete / In Progress / Blocked |

---

## API-Led Architecture View

> Required for Anypoint Visualizer verification (PM Global Story 5).
> Shows which API applications sit at each layer and how they connect.

```
Experience Layer (consumer-facing):
  {consumer}-exp-api
    ↓ calls
Process Layer (orchestration + business logic):
  {domain}-proc-api
    ↓ calls
System Layer (one backend per system API):
  {system1}-sys-api       {system2}-sys-api
       ↓                        ↓
  {System 1}              {System 2}
```

| API Application | Layer | Naming Convention | anypoint.platform.visualizer.layer |
|----------------|-------|------------------|-----------------------------------|
| {name} | system / process / experience | {system}-sys-api / {domain}-proc-api / {consumer}-exp-api | System / Process / Experience |

**Cross-layer violation check:** Experience APIs must NOT call System APIs directly. Process APIs mediate all orchestration.

---

## Level 4 — Operational Needs

**Selected:**

- [x] anypoint-monitoring-basic
- [ ] anypoint-monitoring-custom-dashboard
- [ ] business-events (audit/KPI)
- [ ] external-observability ({platform})
- [ ] email-notifications
- [ ] sms-notifications
- [ ] slack-notifications
- [ ] teams-notifications
- [ ] scheduling (cron/fixed-frequency)
- [ ] watermarking (incremental sync) — if checked: scheduling.watermarking=true + persistent Object Store MANDATORY
- [x] dlq-and-retry
- [ ] field-level-encryption — which fields: {list}
- [ ] data-masking-in-logs — which fields: {list} (authoritative field list is in Level 5 security table)
- [ ] flow-control (rate-limiting, backpressure)
- [ ] invalid-message-channel — queue: {domain}-invalid-messages-queue

---

## Level 5 — Security

**Level:** {internal / partner / regulated / government}

| Control | Value |
|---------|-------|
| API Auth | {client-id / oauth2-client-credentials / jwt / mtls} |
| Gateway Policies | {list: client-id-enforcement, rate-limiting, jwt-validation, etc.} |
| Secrets Manager | {yes / no} |
| Field Encryption | {yes / no — which fields} |
| Data Masking in Logs | {yes / no — which fields} |
| mTLS | {yes / no} |

---

## Level 6 — Client-Facing Needs

- [ ] operations-dashboard
- [ ] business-reporting
- [ ] audit-trail
- [ ] self-service-portal
- [ ] ux-frontend
- [x] none (backend only)

---

## Scaffold Profile

**Profile:** {minimal / standard / enterprise / regulated}

**Selection rationale:**

| Trigger Condition | Present? |
|------------------|----------|
| security=regulated or government | Yes / No |
| availability=99.99 | Yes / No |
| customDashboard=true | Yes / No |
| compensationStrategy=compensating-transaction | Yes / No |
| security=internal or partner AND availability=99.9 | Yes / No |

---

## Flow Architecture

> One section per flow in decisions.json flows[].
> These sections feed the scaffold generator — be precise.

---

### Flow {N}: {flow-name}

| Property | Value |
|----------|-------|
| Flow Name | `{action}-{entity}-flow` (kebab-case) |
| API-Led Layer | system / process / experience |
| Source System | {system} |
| Target System | {system} |
| Trigger | http / scheduler / platform-event / mq-subscriber / cdc / sftp / db-poll |
| Direction | unidirectional / bidirectional |
| Error Strategy | retry-then-dlq / fail-fast / write-off |
| Compensation | retry / write-off / compensating-transaction |
| Compensation Rationale | {Mutates financial/provisioning/compliance? → saga. Idempotent + transient? → retry. Write-off: {reason}} |
| Saga Steps (if compensating-transaction) | {Step 1: action → Step 2: rollback trigger → Step 3: compensating action} |
| File | `src/main/mule/{domain}-flows.xml` |
| DWL File | `src/main/resources/dwl/{verb}-{source}-to-{target}.dwl` |
| MUnit File | `src/test/munit/{flow-name}-test.xml` |

**Description:**
{What this flow does in business terms. 1-3 sentences.}

#### Field Mapping Table

> Scaffold generator reads this table to generate TODO comments in .dwl stubs.
> Status column drives which comment type is generated:
>   Confirmed → one-line DWL comment: // {sourceField} → {targetField}: {rule}
>   Open Item → TODO comment:         // TODO [OPEN ITEM]: {question} — confirm with client — best guess: {value}
> Missing rows → generic "implement transformation here" TODO (worst outcome for developer).

| Status | Source Field | Source System | Target Field | Target System | Transform Rule / Open Question |
|--------|-------------|--------------|-------------|--------------|-------------------------------|
| Confirmed | {fieldName} | {system} | {fieldName} | {system} | {direct copy / format "YYYY-MM-DD" / lookup table / concatenate X + Y / conditional: if A then B else C} |
| Open Item | {fieldName} | {system} | {fieldName} | {system} | {question — best guess: {value}} |

#### Business Rules

> Client-confirmed constants, conditional logic, and lookup values that go into DWL.
> Developer copies these directly into DWL comments.
> Source: prd.md, intake docs, client-confirmed questions.

- **{Rule name}:** {description of the rule with exact values. E.g. "Tax code defaults to 'TX-STD' unless customer.state == 'CA', then use 'TX-CA'"}
- **{Rule name}:** {rule}
- **{Rule name}:** {rule}

#### Open Items

> Fields or rules the architect could NOT determine from intake docs alone.
> Developer resolves with client during Sprint 1.
> Do NOT block architecture on these — they become developer-facing TODO comments.

- [ ] **OPEN ITEM:** {question} — best guess: {value or approach}
- [ ] **OPEN ITEM:** {question} — best guess: {value or approach}

---

### Flow {N+1}: {flow-name}

> (repeat section above for each flow)

---

## Semantic Dissonance

> Required when two systems use the same field name for different concepts.
> Document before coding — "notoriously difficult to resolve" after the fact.
> If none found: write "None identified."

| Field | System A Meaning | System B Meaning | Resolution |
|-------|-----------------|-----------------|------------|
| {field} | {what System A means by it} | {what System B means by it} | {map via lookup / transform rule / constant} |

---

## Cross-Cutting Patterns Applied

| Pattern | Applied? | Configuration |
|---------|----------|---------------|
| Idempotent Receiver | Yes / No (required for all async flows) | Key: `{prefix}-{messageId}`, Store: Object Store persistent, TTL: {N}h ({event-class}: critical=168h / standard=24h / CDC=4h / notification=1h) — must match or exceed messageTtlHours |
| Claim Check | Yes / No | Threshold: {size}MB, Store: {S3 / Azure Blob}, Key: correlationId |
| Normalizer | Yes / No | Entity: {entity type}, Formats: {list source formats} |
| Wire Tap | Yes / No | Destination: `{domain}-audit-queue`, Async: yes |
| Correlation ID | Always | Generated at: Experience API layer, Header: X-Correlation-ID |
| Invalid Message Channel | Yes / No | Queue: `{domain}-invalid-messages-queue` |

---

## Flow Control Configuration

| Setting | Value | Rationale |
|---------|-------|-----------|
| Message TTL | {N} hours | {category: critical / standard / notification / CDC} |
| Max Concurrency | {N} | {downstream system capacity constraint} |
| Queue Depth Alert | {N}% | Default 80% |
| Backpressure Enabled | yes / no | |
| Deduplication Enabled | yes / no | |
| Deduplication TTL | {N} minutes | Must = messageTtlHours × 60. Critical=10080, Standard=1440, CDC=240, Notification=60 |
| Push / Pull direction | push / pull / hybrid | {rationale} |

**Queue Depth Monitoring Alerts:**
| Threshold | Severity | Action |
|-----------|----------|--------|
| > 80% of max depth | MEDIUM | Alert Ops — consumer may be slow |
| > 90% of max depth | HIGH | Page on-call |
| 100% (full — tail drop active) | CRITICAL | Page on-call — messages being dropped |
| DLQ depth > 0 | HIGH | Page on-call immediately |

---

## Error Handling

| Property | Value |
|----------|-------|
| Strategy | retry-then-dlq / fail-fast / retry-only |
| Compensation | write-off / retry / compensating-transaction |
| Max Retries | {N} |
| Backoff | fixed / exponential |
| DLQ Name | `{domain}-{action}-{env}-queue-dlq` |
| Retry Queue Name | `{domain}-{action}-retry-queue` |
| Invalid Message Channel | `{domain}-invalid-messages-queue` |
| Error Envelope | yes (always) |

**Compensation rationale:**
{Why this compensation strategy. Reference: did it mutate financial/provisioning/compliance data? Is it idempotent and transient?}

---

## DevOps

| Property | Value |
|----------|-------|
| CI/CD | github-actions / azure-devops / jenkins / none |
| Environments | dev / uat / prod |
| Runtime | Mule 4.8.0 / Java 17 |
| Deployment | cloudhub2 / runtime-fabric / hybrid |
| Region | {us-east-1 / eu-west-1 / ap-southeast-1 / etc. — match data residency constraint from prd.md} |
| MUnit Required | yes |
| MUnit Coverage Floor | {80 / 75 / 60}% (per pattern standard) |
| Exchange Publish | yes / no |

---

## AI Integration

> Complete this section when decisions.json aiIntegration.enabled=true (patterns P, Q, or R).
> If AI integration is not used, write "Not applicable — no AI patterns in this project."

| Property | Value |
|----------|-------|
| Enabled | yes / no |
| Provider | openai / anthropic / bedrock / gemini / azure-openai |
| Model | {model name — e.g. gpt-4o, claude-sonnet-4-6} |
| Use Case | extraction / classification / semantic-routing / enrichment / embedding / generation |
| Timeout | {N} seconds (default 30; adjust for embedding/generation workloads) |
| Fallback on Timeout | yes / no — {fallback behavior: use cached result / continue with default / skip enrichment} |
| Embeddings Store | pinecone / weaviate / pgvector / opensearch / none (pattern Q only) |

**Rationale:**
{Why AI is needed mid-flow and not as a pre/post-processing step. What the LLM call does that a rule engine cannot.}

**Failure handling:**
{What happens if the LLM call fails or times out. This must be a write-off or retry, never compensating-transaction — AI calls are not financial mutations.}

---

## Decisions Log

> Decisions made during this architecture session that are non-obvious or deviated from defaults.
> Reference: PLANNING_CONTEXT.md sections consulted. FIELD_KNOWLEDGE.md entries applied.

| Decision | Chosen | Alternatives Considered | Rationale |
|----------|--------|------------------------|-----------|
| {topic} | {choice} | {alt1, alt2} | {why chosen} |

**FIELD_KNOWLEDGE.md entries applied:**
- FK-{NNN}: {title} — {how it influenced this architecture}

**PATTERNS_RESEARCH.md consulted:**
- {section} — {what it informed}

---

## Architect Sign-Off Checklist

- [ ] decisions.json written and validated against standards/decisions-schema.json
- [ ] All connectors in registry with lastVerified ≤ 30 days (MCP verify if stale)
- [ ] scaffold.profile written to decisions.json
- [ ] All flows in decisions.json flows[]
- [ ] Field Mapping tables complete for all flows — every row has Confirmed or Open Item status
- [ ] Semantic dissonance documented (or explicitly "None identified")
- [ ] Idempotency (deduplicationEnabled=true) on all async MQ consumer flows
- [ ] deduplicationTtlMinutes = messageTtlHours × 60 (not at schema default of 1440 for critical queues)
- [ ] messageTtlHours set per event category (not defaulted to 24 for critical/CDC events)
- [ ] DLQ ≠ Invalid Message Channel — structural failures route to invalid-messages-queue
- [ ] Compensation strategy documented per flow with explicit rationale
- [ ] Saga compensation steps listed for any compensating-transaction flow
- [ ] EDA fit assessment complete with verdict (if Messaging style selected)
- [ ] Kafka vs Anypoint MQ decision documented if >100K/day or replay needed
- [ ] Anti-patterns table checked — none of the 10 patterns present without documented justification
- [ ] All Open Items entered as developer TODO targets
- [ ] No Open Items that are architecture blockers remain
- [ ] AI Integration section complete or marked "Not applicable"
- [ ] API-Led Architecture View diagram matches decisions.json layers
- [ ] FIELD_KNOWLEDGE.md write-back done (FK-NNN entry added if any non-obvious finding)

---

*Produced by BMAD Architect Agent reading: prd.md + DESIGN_STANDARDS.md + PATTERNS_RESEARCH.md + FIELD_KNOWLEDGE.md*
*Next step: BMAD PM Agent reads this file + decisions.json to generate stories.md*
